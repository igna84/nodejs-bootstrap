const application = require("./classes/core/application");
const config = require("./classes/core/config");

application();

// const config = require("./settings.json");
// const fs = require("fs");

// const banner = fs.readFileSync("./resource/banner.txt");
